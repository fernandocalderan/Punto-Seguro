PORT=3000
ADMIN_PASSWORD=cambia-esta-clave
MAX_PROVIDERS_PER_LEAD=2
EMAIL_FROM=Punto Seguro <no-reply@puntoseguro.local>

# Persistencia:
# - Si DATABASE_URL no existe => almacenamiento JSON local en /data
# - Si DATABASE_URL existe => almacenamiento en Postgres
# - En Vercel/producción, DATABASE_URL es obligatorio para persistencia real
# Nota: si tu proveedor (ej. Neon) te da `?sslmode=require`, puedes endurecerlo a `?sslmode=verify-full`.
# DATABASE_URL=postgresql://user:pass@host:5432/dbname?sslmode=verify-full
# PG_SSL_DISABLE=false
# DATA_DIR=./data

# SMTP opcional (si no se define, se usa outbox local en /data/email-outbox.log)
# SMTP_HOST=smtp.tudominio.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=usuario
# SMTP_PASS=clave
